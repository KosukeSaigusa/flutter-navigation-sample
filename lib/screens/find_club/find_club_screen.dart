import 'package:flutter/material.dart';

class FindClubScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('サークルを探す'),
      ),
      body: const Center(child: Text('サークルを探す画面')),
    );
  }
}
